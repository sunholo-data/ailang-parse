module probe

import std/fs (readFile, mkdirAll)
import std/io (println)

export func main() -> () ! {IO, FS} {
  mkdirAll(".ailang-scratch");
  let content = readFile("docparse/services/format_router.ail");
  println(content)
}
