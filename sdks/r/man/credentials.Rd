% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credentials.R
\name{credentials}
\alias{credentials}
\alias{default_base_url}
\alias{credentials_path}
\alias{load_saved_key}
\alias{save_key}
\alias{resolve_api_key}
\title{Credential storage helpers}
\usage{
default_base_url()

credentials_path()

load_saved_key(base_url = default_base_url())

save_key(
  api_key,
  base_url = default_base_url(),
  key_id = "",
  tier = "free",
  label = ""
)

resolve_api_key()
}
\arguments{
\item{base_url}{Base URL the saved key must match.}

\item{api_key}{API key string (must start with \code{dp_}).}

\item{key_id}{Opaque key identifier returned by the device-auth flow.}

\item{tier}{Plan tier (\code{"free"}, \code{"pro"}, ...).}

\item{label}{Human-readable label for the key.}
}
\value{
\code{default_base_url()} returns a single string.
}
\description{
Mirrors the cross-SDK credential format used by the Python, JavaScript and
Go AILANG Parse clients so a key saved in any one of them is usable from
any other. The on-disk file is JSON with these fields:
\code{api_key}, \code{base_url}, \code{key_id}, \code{tier}, \code{label}.
}
\details{
Storage location:
\itemize{
  \item Linux/macOS: \code{$XDG_CONFIG_HOME/ailang-parse/credentials.json}
        (default \code{~/.config/ailang-parse/credentials.json}).
  \item Windows: \code{\%APPDATA\%/ailang-parse/credentials.json}.
}

On Unix the file is written with mode \code{0600} and the directory with
\code{0700}.
}
\section{Functions}{
\itemize{
\item \code{default_base_url()}: Default hosted API endpoint.

\item \code{credentials_path()}: Absolute path to the credentials file (whether or
not it exists).

\item \code{load_saved_key()}: Load saved credentials matching \code{base_url}, or
\code{NULL} if no compatible record exists.

\item \code{save_key()}: Persist credentials to disk with restrictive
permissions. Returns the path written, invisibly.

\item \code{resolve_api_key()}: Resolve any saved API key — checks
\code{DOCPARSE_API_KEY} first, then the credentials file (without
filtering by base URL). Used by the MCP bridge.

}}
\examples{
default_base_url()
credentials_path()
\dontrun{
load_saved_key()
}
\dontrun{
save_key("dp_your_key_here", label = "my-laptop")
}
resolve_api_key()
}
