{
  "name": "ailang-parse-browser-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright smoke tests for the in-browser AILANG WASM bundle (homepage demo + workbench)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
